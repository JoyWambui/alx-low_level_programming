#include "main.h"

/**
 * _strlen - returns length of a given string.
 * @s: pointer to character.
 * Return: 0 success.
 */



int _strlen(char *s)
{	
	char c = *s;
	char arr[]= c;
	return (sizeof(arr));
}
