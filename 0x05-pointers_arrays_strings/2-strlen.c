#include "main.h"

/**
 * _strlen - returns length of a given string.
 * @s: pointer to character.
 * Return: 0 success.
 */



int _strlen(char *s)
{
	for (int i = 0; *(s + i) != '\0'; i++)
	{
		return (i);
	}
}
